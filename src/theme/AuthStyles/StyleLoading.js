import {
    StyleSheet
} from 'react-native';
import styleVariable from '../StyleVariable';

const StyleLoading = StyleSheet.create({
    welcomeBigText: { fontWeight: 'bold', fontSize: 20, color: styleVariable.mainColor }
});
export default StyleLoading;